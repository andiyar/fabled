# Fabled decisions ledger

Append-only. Format per `~/Developer/fable-kit/decisions-ledger-template.md`: date · decision · why · revisit-when.

- **2026-07-08 · Pure Swift, no Node sidecar.** The Agent SDK just wraps the same CLI; a sidecar adds a runtime and an IPC hop to outsource a codec we can own. Tolerant decoding + fixtures make drift manageable. *Revisit if:* Anthropic breaks the stream-json protocol incompatibly more than ~once a quarter.
- **2026-07-08 · Depend on hidden flag `--permission-prompt-tool stdio`.** Only mechanism that turns auto-deny into interactive `can_use_tool` control requests; it is the SDK's own path, verified working on CLI 2.1.202. *Revisit if:* a CLI update removes/renames it (live tests will catch this immediately).
- **2026-07-08 · Opus subagents implement; coordinator (Fable while credits last, any model after) designs, dispatches, reviews.** Credit economics; the fable-kit method makes it work. Full handoff pack in `docs/superpowers/COORDINATION.md` + plan briefs.
- **2026-07-08 · ClaudeKit is zero-dependency.** SQLite via system C library, markdown via AttributedString, no GRDB/swift-markdown-ui. Keeps the engine auditable and the build trivial. SwiftTerm accepted later, app target only. *Revisit if:* AttributedString markdown proves unusable for code blocks (ledger the swap).
- **2026-07-08 · XcodeGen for the app target (Plan 3).** Agents can't drive Xcode GUI; `project.yml` is reviewable text; `.xcodeproj` becomes generated output. *Revisit if:* XcodeGen goes unmaintained.
- **2026-07-08 · Aesthetic: native macOS structure + Claude warmth in conversation** (serif voice, clay send button). Chosen by Ben over pure-native and dense-power-tool options.
- **2026-07-08 · Name: Fabled.** Chosen by Ben.
- **2026-07-08 · TranscriptEntry reshaped vs the Plan 2 brief.** Corpus census found zero `summary` lines (titles live in `custom-title`/`ai-title`), real prompts inside on-disk `user` lines, and `result` lines that are subagent caches, not turn results. Enum gains `userPrompt`/`title`/`sessionMeta` cases + a `LineContext` struct; `.summary` kept as legacy. *Revisit if:* the CLI changes the on-disk transcript format (the Task 11 drift census will show it).
- **2026-07-08 · Search schema: single FTS5 table, rowid = (file_id << 32) | line_no.** Per-file re-index becomes an indexed rowid range delete — no triggers, no external-content table. `files` carries a `title` column so search hits never re-open session files. Internal, not public API. *Revisit if:* the warm-reindex gate (<1 s) fails at real scale.
- **2026-07-08 · Index scope: human-relevant, main-chain text only.** Non-sidechain prompts (compaction summaries included), non-sidechain assistant text, titles, summaries. Sidechain/subagent traffic, `isMeta` lines, and tool results excluded. Subagent transcript files (`<session>/subagents/`) out of scope for Plan 2 entirely. *Revisit if:* users need to search subagent output.
- **2026-07-08 · Watcher: directory kqueue + 2 s mtime poll, throttled rescan.** Directory kqueue cannot see appends to existing files (POSIX), and appends are the main live signal; per-file kqueue would need 680+ fds. Poll stats depth-2 entries only. Throttle (not restartable debounce) so steady signals cannot starve the rescan. FSEvents stays rejected per the brief. *Revisit if:* poll cost shows up in profiles or fd budgets change.
- **2026-07-08 · JSONValue(parsing:) via JSONSerialization for bulk decode paths.** Several times faster than JSONDecoder for multi-MB transcript files; equivalence with the Plan 1 path is guard-tested over every fixture line. Live-session decoding keeps the original JSONDecoder path. *Revisit if:* the equivalence test ever fails on new fixtures.
- **2026-07-08 · Transcript perf gate amended 500 ms → 1 s.** Measured 634 ms, dead stable across runs, to fully decode the corpus-largest session (52.5 MB, 16,211 entries) after eliminating per-line copies (JSONLines slices). Cost is JSONSerialization-parse + bridge bound (~39 µs/line, ~83 MB/s); the 500 ms budget implicitly assumed a ~25 MB max file and the real max is 2×. Beating it needs a different decode architecture (custom parser / lazy raw payloads) — not warranted before Plan 3 UX proves the need. 1 s still trips on a JSONDecoder fallback, a double-parse, or quadratic behavior. *Revisit if:* Plan 3 transcript-open feels slow, or max file size keeps growing.
- **2026-07-09 · `updatedInput` is mandatory in allow responses.** Probed live: the CLI Zod-validates the allow payload and omitting `updatedInput` denies the tool (fixtures/2026-07-09-perm-allow-noinput.jsonl). `PermissionDecision.allow` reshaped to `(updatedInput:, updatedPermissions:)`; encoding echoes the request's input when nil. *Revisit if:* the CLI relaxes the schema (fixture re-record will show it).
- **2026-07-09 · Model picker is catalog-driven.** The initialize control_response carries a full `models` array (value, resolvedModel, displayName, effort caps); the picker renders it + a free-text custom ID. Replaces the brief's static alias list. *Revisit if:* the catalog disappears from the handshake.
- **2026-07-09 · Sidebar history is index-backed; the index title is authoritative.** `SearchIndex.sessionSummaries()` serves the whole list from the files table (instant vs 3.7 s derivation walk) and resolves the Plan-2 title-divergence follow-up in the index's favor. `SessionStore.sessions(in:)` remains for store-only consumers. *Revisit if:* index staleness becomes visible in the sidebar.
- **2026-07-09 · View models live in FabledCore (SPM), views in the app target.** ChatSession/AppModel/reducer are `swift test`-covered; the xcodeproj holds only SwiftUI bodies. Transport injected via `AgentConnection` closures. *Revisit if:* the split forces awkward API surface.
- **2026-07-09 · Resumed sessions seed their timeline from disk.** Probed live: `--resume` replays nothing on the stream-json wire (context survives server-side). Resume/Fork load the transcript through the reducer, then attach the live stream. *Revisit if:* a CLI update starts replaying (isReplay-flagged events would double-render — the seed guard would need dedupe).
- **2026-07-09 · Plan 4 splits into three sequential plans (4a/4b/4c), expanded one at a time.** 18 features ≈ 30+ tasks — one document would be unreviewable and violates the "working software per plan" rule. 4a = interactive surfaces & side inspector (features 2, 3, 1, 4, 5, 17 — AskUserQuestion is functionally broken today, so surfaces go first); 4b = shell & signals (13, 14, 15, 16, 18, 7, liveness from 11 — every Plan 3 gate irritant in one sweep); 4c = lifecycle, terminal & presets (6, 8, 9, 10, rest of 11, 12). Tech-debt riders travel with their layer as briefed. *Revisit if:* 4a's gate shows the split boundaries fight each other.
- **2026-07-11 · Cowork descope: Fabled is pure Claude Code for now.** Ben (2026-07-10, CD-UI digest conversation): "I don't know that I want cowork necessarily. i think we can do a pure CC for now." Feature 9 (Cowork preset) is dropped from 4c; feature 8 (Chat preset) is deferred pending Ben's explicit confirmation (also non-CC). 4c keeps terminal, session management, resilience, and app identity. *Revisit if:* Ben asks for chat/cowork surfaces after daily-driving Fabled.
- **2026-07-11 · Plan 4b scope set at expansion (conscious deltas vs the 2026-07-09 brief).** In: features 13, 14, 15, 16, 18, 7, liveness from 11 (per the split), PLUS session effort control + thinking-delta rendering (Ben's FASTER priority — measured first-text 24s→17s with `--effort medium`; FOLLOWUPS 2026-07-10), transcript step-grouping (CD digest §2a — biggest transcript-hygiene delta), checklist re-plumb from TodoWrite to TaskCreate/TaskUpdate (TodoWrite is gone from this CLI config; card reused as-is), a design-language phase up front (Ben: "attractive, sexy, mac-assed" — 4b builds the most visible chrome, so tokens/materials/icon land first, not as repaint). Feature 15 rescoped to disk reality: subagent transcripts now live under `<session>/subagents/` and are ALREADY invisible to the depth-2 enumeration — the deliverable becomes historical subagent drill-down (meta.json carries the parent toolUseId) + a guard test pinning the depth-2 invariant; sidebar noise from probe/worktree project dirs is handled by feature 18's filters, not special-casing. *Revisit if:* the 4b gate shows the scope was too fat (split like Plan 4 did).
- **2026-07-11 · Post-4b roadmap: gate carve-outs → permission hotfix → design sprint → friction-first 4c (Ben: "do it").** Born of Ben's faith-check + UX audit (session 2b161833; full trail in `UX-LEDGER.md`). (1) The in-flight 4b plan executes unchanged — the plan is the interface. (2) The 4b gate additionally covers: typeface/token values (T5 invites adjustment), light/dark adaptivity of the new hex tokens, and whether the inspector Back-trail rider is actually satisfied by gate item 8. (3) Immediately after 4b merges: **permission-mode spawn hotfix** — no code path passes `--permission-mode` at launch (`newSession`/`resume` never set it; the toolbar control op is unvalidated by the CLI), which is the root cause of Ben's July 9 "permissions has done absolutely nothing" and bypassPermissions still prompting. Bug-sized, does not wait for 4c. (4) Between the 4b gate and 4c expansion: a **design/interaction sprint** — HTML mockups Ben reacts to directly, settling the look (mac-assed application scope, digest §6 residue) AND the interaction model (type-to-resume, composer-adjacent model/permission chips, curated "Auto" permission modes) as DECISIONS entries before any 4c code is planned. Same logic §6 applied to 4b: define before building, don't repaint. (5) **4c is re-prioritized friction-first**: session-continuity cluster (sticky model+mode on resume, type-to-resume, composer chips, curated modes, tags) leads; terminal/diff/background panels follow. The 4c expansion MUST consume every OPEN row of `UX-LEDGER.md`; demoting a row requires a DECISIONS entry with Ben's sign-off. *Revisit if:* the 4b gate reveals structural (not tunable) design problems — then the sprint moves earlier and widens.
- **2026-07-09 · `--resume` continues the SAME session ID — no fork-on-resume (probed, CLI 2.1.205).** Dead or live, `--resume <id>` reuses the id and appends to the same transcript file; only `--fork-session` mints a new identity. Two processes on one id interleave appends without corruption (line-atomic), so the one-process invariant guards *semantic* history cleanliness, not data integrity. The gate's "fork-like new session ID" observation was a UI-level artifact (Fabled adds a second live row next to the history row), which is exactly what feature 16 should fix: Continue = merge into the same identity; Fork = labelled branch. Fixtures: 2026-07-09-resume-collision-{A,B}.jsonl. *Revisit if:* a CLI update changes resume identity semantics (re-run fixtures/probe_collide.py). follow the user's `claude` symlink; detect drift from the install path at spawn.** The 2.1.204→2.1.205 auto-update retargeted `~/.local/bin/claude` mid-day and live Fabled sessions followed it (Claude Desktop was unaffected because it pins its own copy). Desktop-style pinning was considered and rejected: the installer garbage-collects old `versions/<v>` binaries (only 3 survive today), so a pinned path eventually dangles — a worse failure than drift — and a pinned Fabled silently diverges from the CLI Ben uses in the terminal (sessions, settings, features). Mitigations instead: (a) tolerant decoding stays the wire insurance, (b) `SessionConfiguration.resolveClaudeVersion()` reads the version out of the resolved symlink path at spawn and ChatSession warns immediately — `system init` can no longer be the trigger, 2.1.205 defers it until the first user turn, (c) fixtures re-record in minutes via `fixtures/probe_*.py`. *Revisit if:* a CLI update breaks the protocol outright (not just adds shapes) — then vendor a known-good binary per Fabled release.
- **2026-07-11 · `total_cost_usd` is session-cumulative — assign, never sum.** Verified across slashfx + control-ops fixtures (monotonic growth; synthetic num_turns:0 results echo it unchanged). ChatSession had double-counted since Plan 3; fixed in 4b T2 fix loop. *Revisit if:* a CLI update makes result costs per-turn again (the testCostTracksWireCumulative pin will catch it).
- **2026-07-12 · Fabled design language locked WITH Ben (co-design mockup sprint, session e1cf0e99).** After the 4b gate where design "failed loudest" and Ben's charge was that he was never consulted, the look was co-designed through HTML mockups he reacted to in ~6 rounds (hosted artifacts + `scratchpad/fabled-directions.html`, `fabled-conversation.html`; consider committing into `docs/superpowers/design/`). Ben drove every call. **Palette (mode-aware — follows the system appearance):** dark = **Teal Midnight** (ground `#0C1618`, panels `#122120`, bronze `#CFA669`/`#D8AE6D`, code-glow cyan `#5AC9B4` for live/attention); light = **Linen** (ground `#F5F4EF` — the halfway Ben picked between warm "Bone" and neutral "Paper"; bronze accent `#A9703B`, amber `#B2751C` for needs-you, slate-blue `#4E6E8C` for ready-to-review). Teal-tinted light was rejected as too cold, cream as too warm. Bronze (the harp) is the organising accent, NOT teal. **Type:** New York (`ui-serif`) for the "Fabled" wordmark, SF (`-apple-system`) for UI, SF Mono for code — native by intent, not laziness. **Home screen** = an attention inbox: a "Waiting on you" list with plain-English status labels ("Needs your reply"/"Ready to review") + a preview line of what each session wants, a "Lately" list, a persistent **Home** affordance in the toolbar, and a **composer** with model/effort/permission(**Auto**) chips at the bottom by the text box; the composer is the front door (type-to-resume). **Conversation view** = collapsed step-group summary rows ("Read 4 files ›", "Ran 3 commands ▾", bronze-accented subagent groups) that expand inline; edits show `+N −N` inline; the right panel **defaults to an Activity/task list** (live build pulsing + finished agents/commands/edits/reads as summary rows) where **each row drills into its detail with a Back that returns to the list**; the composer **grows as you type** (to ~8 lines then scrolls, never a horizontal run-on); a quiet git footer strip (branch · session ±diff · time · cost · Create PR). *Why:* reverses "generic, boring SwiftUI" and the "sick of being ignored" charge by settling the look before any Swift, in Ben's terms. Decides UX-LEDGER rows 7, 11, 16, 17, 18, 19, 21, 22, 23, 24, 25 at the design level, and generalizes the CD background-tasks panel (digest §2c). *Revisit if:* Ben reacts against a screen in-build — rows CLOSE only when he verifies in a real build, not here; and the build must still make step-grouping survive permission-gate interleaving (row 25 root cause) and turn these mockup values into Theme tokens.
- **2026-07-12 · Sidebar + tags settled (same co-design sprint).** **Attention is a section, not a badge** (fixes row 24's useless lone tick): "Needs you" (reply/review rows with a preview line) and "Working" (a live, *pulsing* dot) float above all grouping; idle rows carry no decoration (silence = nothing needs you). **Two-level grouping** — a primary axis + a sub-axis, driven by the funnel (Group by / Then by / Sort within); **default Date › Project** (Ben's CD habit + he runs multiple projects at once), any pair allowed. **Pin + Archive are per-row** (right-click / hover), NOT multi-select. **Multi-select exists but is scoped to tagging** — select many → Tag… (Archive folded in since free); Ben judged a general bulk-action system "more effort than it's worth." **Tags:** plain text chips — **colour is reserved for projects only** (Ben: colour on both is "noisy"); the tag picker is **searchable with per-tag session counts**; starter set bug/review/design/release + freely user-created; **rename + delete** via an "Edit → Manage" flip, and **deleting a multi-use tag asks first** (undoable), removing the tag everywhere but keeping the sessions; tags are also editable from inside a session next to the title. Filter by tag via a top chip row; **multiple selected tags = AND** (narrow in). Explicitly tuned for Ben's real workload — **long-form creative + PhD tagging** (characters, scenes, chapters, papers, lit-review), not just dev tags. Storage: the custom-title sidecar store. *Why:* closes UX-LEDGER rows 6 (sidebar sorting), 13 (tags — the cut one), 24 (useless badge) at the design level, generalizes activity filters (row 10). *Revisit if:* Ben reacts against it in-build — rows CLOSE only on build-verify.
- **2026-07-12 · 4c Work Stream B plan LOCKED — three scope calls (Ben).** Reviewing the DRAFT plan (`plans/2026-07-12-4c-work-stream-b.md`) before locking, Ben decided: (1) **Row 15 last-model shows on the historical session page**, not the sidebar rows — the sidebar `.prow` stays title + time only (mockup-faithful); the model appears exactly where he decides whether to resume. (2) **Create PR is omitted from v1** of the git footer strip — the strip ships read-only (branch · ±diff · cost); no PR button, no `gh`/network affordance at all this pass. This partially defers UX-LEDGER row 28's "· Create PR" clause with Ben's explicit sign-off (ledger rule 3). (3) **`Screenshots for GUI etc/` → added to `.gitignore`** (row 20) — makes the prior intentional untrack explicit; 46 MB / 27 PNGs stay out of git history now the design is captured in the committed mockups. Also corrected at lock: the plan **pushes** the `plan-4c-work-stream-b` branch (Ben: "push is fine"); git identity is already `andiyar@gmail.com`, not the stale `Moonlit-Studio.local` the handoff/memory claimed. *Revisit if:* Ben wants Create PR sooner, or the screenshots become load-bearing enough to version (git-lfs then). NOTE (plan-level, not a Ben taste-call): tag storage is a **dedicated `tags.json`**, NOT "the custom-title sidecar store" of the 2026-07-12 "Sidebar + tags settled" entry — that store is read-only (custom titles come from CLI-written transcript lines); corrected in the plan's invariant 2 / B4.2. To be confirmed with Ben at the B4 build.
- **2026-07-12 · Dual-use / multi-folder sessions added to 4c (Ben, session e87008fa).** While approving the home mockup, Ben asked whether a session can span more than one folder ("a dual use session"). Verified: Claude Code 2.1.206 has `--add-dir <directories...>` ("additional directories to allow tool" access); Fabled today is single-folder only (`SessionConfiguration.workingDirectory`; no `--add-dir`; the picker is single-select — `RootView.swift:30`). Ben's call: **build it in 4c** — a session takes a **primary** folder + one or more **extra** folders, passed as `--add-dir`, surfaced on the composer's folder chip. Directly serves his first-class creative/PhD dual-use (manuscript + research notes; thesis chapter + data — [[ben-creative-phd-use]]). Tracked as plan task B1.6 + UX-LEDGER row 33 — a scope addition on the record, not silent (ledger rule 3). *Revisit if:* it proves bigger than a bounded add (then split resume-carry of the extra dirs into its own follow-up — resume rebuilds config from the transcript, which doesn't record added dirs).
